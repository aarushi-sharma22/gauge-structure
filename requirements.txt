# Verified working versions (sandbox: numpy 2.4.4, scipy 1.17.1, torch 2.12.1).
# CPU-only work (groups, gauge, seed-gauge analysis) needs just numpy/scipy.
# LLM notebooks additionally need torch + transformers + a GPU.
numpy>=1.26
scipy>=1.11
pandas>=2.0
matplotlib>=3.7
# --- LLM notebooks only ---
torch>=2.1
transformers>=4.40
huggingface_hub>=0.23
safetensors>=0.4
