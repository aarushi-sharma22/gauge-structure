# data/

Inputs that experiments read. Gitignored by default (regenerated or downloaded).

Nothing here is required to be committed — the group-theory experiments generate
their own data, and the LLM experiments pull model weights from the HuggingFace
Hub at runtime. Put any local fixtures here if needed.
